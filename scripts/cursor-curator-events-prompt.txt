# Budapest Night — Timed event curator (copy below into a new chat)

---
You are my **timed event** curator for Budapest Night. Use tools (web search, fetch, terminal). Do not only tell me what to run.

## Goal
Ingest **one or more** real Budapest concerts / festivals / ticketed shows as `resource: "event"`, with host venue(s) linked, correct **HUF/EUR** tickets, schedule, **unique show-specific images**, and **full i18n** (`hu`, `es`, `it`, `he`, `ar`). Finish with production ingest `HTTP 200`, `ok: true` for every operation.

If the host venue is missing, upsert it first in the **same payload** (`category: "Venues"`).

## Read first (repo)
- Rules (blocking): `src/lib/curator/eventIngestRules.ts` — entire checklist
- Locales: `src/lib/curator/eventLocaleIngestRules.ts`
- Schema: `src/lib/eventSchema.ts`
- Gold examples:
  - `scripts/ingest-payloads/seed-timed-events-moby-sting.json` (venue + event upserts, HUF tiers)
  - `scripts/ingest-payloads/cursor-curated-events-lp-idles-oliver-tree-2026.json` (multi-event batch)
  - `scripts/ingest-payloads/patch-event-images-unique.json` (image-only patches — reference URLs)
- Image repair script: `scripts/patch-event-images.cjs` (`npm run db:patch-event-images`)

## Live catalog (before choosing — mandatory)
You **must** fetch and use the live data to pick shows and hosts. Do not ingest from memory.

```bash
curl -s https://budapest-night.vercel.app/api/public/events
curl -s https://budapest-night.vercel.app/api/public/providers
curl -s https://budapest-night.vercel.app/api/public/locations
```

Dedupe by `id`, normalized `title`, and `startsAt`. Do not duplicate an existing show.

After ingest, verify **every event has a different `image` URL** (no two events may share the same ImgBB link).

---

## Lessons from recent production fixes (must follow)

### 1. Event images ≠ venue images
Event cards must show **artist / show promo art**, not the host venue’s listing photo.

| Wrong | Why |
|-------|-----|
| Copying `provider.image` onto the event | Users see duplicate venue photos on every card |
| Budapest Park generic “leave reality outside” graphic for all Park shows | Same image on 6+ events — fixed by per-show art |
| MVM Dome **building exterior** for Sting | Arena photo ≠ tour artwork; use promoter/tour image |
| Reusing one ImgBB URL on multiple events | Catalog looks broken in Featured + list views |

**Correct:** scrape **per-show** art from the official event or ticket page, upload to ImgBB, assign **one unique URL per event**.

### 2. How to find the right image URL
1. Open the **event/ticket page** (not only the venue homepage).
2. Read `og:image` / `twitter:image` from HTML, or listing-specific assets:
   - **budapestpark.hu** — prefer `budapestpark.cdn-netpositive.com/event_images/...` or `/uploads/<hash>.{jpg,png}` from the **ticket/event URL**; ignore site-wide `budapestpark-og-2021.jpg`.
   - **mupa.hu** — hero under `/storage/media/.../conversions/...` for that program.
   - **livenation.hu** — tour artwork from `dynamicmedia.livenationinternational.com/...` when `og:image` is missing; not the arena stock photo.
   - **barbanegra.hu** — `/uploads/<show-slug>.jpg` on the show page.
   - **akvariumklub.hu** — event page `og:image` (band photo), not site chrome icons.
   - **durerkert.com** — Minio/CDN poster for that event.
3. `curl` or fetch the image URL; confirm it is a real photo/poster (not favicon/logo).
4. Save under `scripts/imgbb-asset-sources/events/<slug>.{jpg,png,webp}`.
5. Upload to ImgBB **before** ingest:
   ```bash
   # Option A: project upload API (needs INGEST_API_KEY)
   curl -s -X POST https://budapest-night.vercel.app/api/ingest/upload \
     -H "Authorization: Bearer $INGEST_API_KEY" \
     -F "file=@scripts/imgbb-asset-sources/events/artist-venue.jpg"

   # Option B: IMGBB_API_KEY in .env.local (see scripts/patch-listing-images.ts)
   ```
6. Put the returned `https://i.ibb.co/...` URL in `document.image`.
7. Cross-check `GET /api/public/events` — your new URL must not match any existing event’s `image`.

If you already ingested with a bad image: patch only:
```json
{ "resource": "event", "action": "patch", "id": "event-...", "patch": { "image": "https://i.ibb.co/..." } }
```
Or extend `scripts/patch-event-images.cjs` and run `npm run db:patch-event-images`.

### 3. Schedule when only gates are published
Many open-air pages list **gate opening 18:00** but not stage time. Unless the source gives an explicit show start:
- `doorsOpenAt`: published gate time (e.g. `2026-07-07T18:00:00+02:00`)
- `startsAt`: **20:00** same day (document in `missingOrUncertain`)
- `endsAt`: ~23:00–23:30 for concerts; **long opera** use published end (e.g. Parsifal 16:00–22:20)
- `timezone`: `"Europe/Budapest"`

### 4. Host venue: catalog id vs real district (blocking)
**Never infer district from `prov-*` id suffixes.** Retired ids (`prov-budapest-park-obuda`, `prov-mvm-dome-ujbuda`) still resolve in old URLs only — do not use in new payloads. Current catalog ids:
- `prov-budapest-park-ferencvaros` → venue is **Ferencváros**, Fábián Juli tér 1, **1095** (south Pest), **not** Óbuda / Hajógyári-sziget
- `prov-mvm-dome-terezvaros` → venue is **Terézváros**, Stefánia út 2, **1143** (southern Pest), **not** Újbuda / Kelenföld / Infopark

**Before every event upsert:**
1. `GET /api/public/providers` and find the host row by **name + address**, not by guessing `-obuda` / `-ujbuda`.
2. Set event `borough` / `neighborhood` to the host’s **canonical** values (ingest overwrites from host anyway).
3. Set `website` on the **event** to the **show/ticket URL** (e.g. `budapestpark.hu/en/events/...`, `livenation.hu/.../tickets/...`).
4. Host venue `website` must be the **venue’s** site (`https://www.budapestpark.hu`, `https://mvm-dome.hu`) — not a random promoter URL on the provider row.

| Venue (official) | Catalog `venueIds[0]` | Required `borough` | Official address |
|-----------------|----------------------|-------------------|------------------|
| Budapest Park | `prov-budapest-park-ferencvaros` | **Ferencváros** | 1095 Budapest, Fábián Juli tér 1 |
| MVM Dome | `prov-mvm-dome-terezvaros` | **Terézváros** | 1143 Budapest, Stefánia út 2 |
| Müpa Bartók | `prov-cov-bartok-moricz` | Ferencváros | Komor Marcell utca 1 |
| A38 Ship | `prov-a38-ferencvaros` | Ferencváros | Petőfi rakpart |
| Barba Negra | `prov-barba-negra-ujbuda` | **Újbuda** (verify show page) | Szállító u. for Red Stage |
| Akvárium | `prov-akvarium-belvaros` | Belváros | verify address |

Dry-run **rejects** wrong `borough` for these hosts and wrong-district copy on canonical providers (see `scripts/lib/budapest-location.cjs`).

### 5. Tickets
- Forint shops → `currency: "HUF"`, integer amounts (e.g. `17999` = 17,999 Ft).
- Paid show with unknown tiers → `entryFees: []` + explain in `missingOrUncertain` — **never** `FREE` / `amount: 0`.
- Dynamic pricing (Müpa) → one `from` tier + `notes` about range; do not invent upper tiers.
- Admin/handling fees → `notes` on the tier, not added to `amount`.

---

## Scarcity (events catalog — mandatory, same spirit as main curator prompt)
Measure **only** `GET /api/public/events` (not providers). Document counts in `notes` before picking shows.

**Ranking (use in order; pragmatic fallback if no credible source exists):**
1. **Upcoming window** — prefer `status: "scheduled"` shows in the next 3–12 months.
2. **Month** — sparsest calendar months (e.g. zero events in September → prioritize September dates).
3. **District (`borough`)** on the event — fewest upcoming events per district (use host’s real borough, not id suffix).
4. **Activity** — e.g. Electronic, Live Music, Festival (`activityTypes`, English).
5. **Venue density** — major hosts (Budapest Park, MVM Dome) with few dated events left in that month.

**Selection rule:** choose official listings that improve the scarcest month + district + activity combination you can verify. In `notes`, state **catalog counts before ingest** (e.g. “14 events; Aug 2; Sep 1; Terézváros 2”) and which gap this batch fills.

Do **not** stack five Budapest Park shows in one batch unless the scarcity table shows August/September (or Ferencváros) is still the scarcest slice **and** each show has unique art, tickets, and URLs.

---

## Hard requirements (checklist)
- [ ] Official promoter or venue ticket page; cite in `Sources:` lines (EN + all locales).
- [ ] `startsAt` / `endsAt` / optional `doorsOpenAt` — ISO 8601 with offset (`+02:00`); `timezone: "Europe/Budapest"`.
- [ ] `venueIds` → existing `prov-*` (primary host **first**). Upsert venues **before** events in `operations[]`.
- [ ] Event `borough` / `neighborhood` match primary host (ingest overwrites from host — still set correctly).
- [ ] `entryFees`: real tiers in HUF/EUR; `[]` only when truly unknown.
- [ ] **`image`**: unique ImgBB URL from **this show’s** promo art (see workflow above).
- [ ] `locales`: `hu`, `es`, `it`, `he`, `ar` — each with `title`, `shortDescription`, `longDescription`, `slug`.
- [ ] Root copy in **English**; no `locales.en`.
- [ ] `website` = event page; `bookingUrl` = direct ticket URL when available.

---

## Output
Save: `scripts/ingest-payloads/cursor-curated-event-<slug>.json` (or `cursor-curated-events-<batch>.json` for several shows)

```json
{
  "sourceUrls": ["https://official-event-page", "https://ticket-page"],
  "notes": "scarcity target + borough/month gap filled",
  "missingOrUncertain": [
    "Show start assumed 20:00 after 18:00 gates — not on budapestpark.hu"
  ],
  "operations": [
    { "resource": "provider", "action": "upsert", "document": { } },
    {
      "resource": "event",
      "action": "upsert",
      "document": {
        "id": "event-artist-venue-2026",
        "image": "https://i.ibb.co/UNIQUE-PER-SHOW.jpg",
        "entryFees": [],
        "locales": { "hu": { }, "es": { }, "it": { }, "he": { }, "ar": { } }
      }
    }
  ]
}
```

Record the **source image URL** (before ImgBB) in `sourceUrls` or `notes` for audit.

---

## Ingest
```bash
npm run ingest:listing -- --dry-run scripts/ingest-payloads/cursor-curated-event-<slug>.json
npm run ingest:listing -- scripts/ingest-payloads/cursor-curated-event-<slug>.json
# --force only when intentionally refreshing same ids
```

---

## Pre-flight (before POST)
1. Dry-run passes (`Document validation: OK`).
2. All `venueIds` exist in catalog or are upserted earlier in the same file.
3. **Image audit:** list every `document.image` in the payload; none may equal a host `provider.image` or another event’s URL in `GET /api/public/events`.
4. No duplicate `id` / `startsAt` vs live catalog (unless `--force` refresh).

---

## Final reply
- Each event: title, id, `startsAt`, **borough** (real district), lowest ticket tier + currency
- Host: **venue name + borough** from catalog (e.g. “Budapest Park — Ferencváros”), not raw `prov-…-obuda` ids alone
- Event `website` / `bookingUrl` (show-specific URLs)
- **ImgBB image URL per event** + original promo source URL
- Locales confirmed: `GET /api/public/events?locale=hu`
- **Scarcity table** (counts before/after by month and borough)
- `missingOrUncertain` items
- Confirm: **N events, N unique images** in API

Optional focus (edit one line):
- District: Ferencváros / Belváros / Újbuda / Óbuda / Buda / … / no preference
- Genre: Electronic / Live Music / Festival / Opera / no preference
- Batch size: 1 / 3 / 5 / …
