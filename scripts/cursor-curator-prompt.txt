# Budapest Night — Cursor curator (copy everything below the line into a new chat)

---
You are my Budapest Night curator agent. Do ALL of the following in this order. Use tools yourself (web search, fetch, terminal). Do not only tell me what to run.

## Goal
Find exactly one new Budapest nightlife or culture listing that fits Budapest Night and successfully ingest it into production.

Valid listing types:
- **Providers** (venues / programs): category must be one of **Events**, **Parties**, **Restaurants**, **Cafés**
- **Culture circles** (community groups): `resource: "meetupGroup"` with a supported `groupType`

Prefer official sources (venue `.hu` site, ticket office, cultural institution, or the organization’s own page). It must be clearly relevant to **Budapest**, with district (`borough`) + canonical **neighborhood** when possible.

## Hard requirements
1. You must finish with a **successful production ingest** (`HTTP 200`, response `ok: true` from `POST /api/ingest`).
2. You must include a **non-empty** raster image URL in the final payload:
   - Providers: non-empty `image`
   - Meetup groups: non-empty `coverImageUrl`
3. Every non-empty image URL in the payload must be an **https ImgBB URL** (`https://i.ibb.co/...` or another host accepted by the repo: `ibb.co`, `image.ibb.co`, or `*.ibb.co`).
4. Do **not** complete the task with `image: ""`, `coverImageUrl: ""`, or omitted cover when ingesting a meetup. If no usable official image exists, find a different listing.
5. Do **not** invent prices, hours, addresses, phone numbers, emails, image URLs, ratings, review counts, or activity tags not supported by the source.
6. Work in the **budapest-night** repo only. Default ingest target is **https://budapest-night.vercel.app** (not ClassScout).

## First: inspect the live catalog and compute scarcity
Before choosing a candidate, fetch:
- `GET https://budapest-night.vercel.app/api/public/providers`
- `GET https://budapest-night.vercel.app/api/public/meetup-groups`
- `GET https://budapest-night.vercel.app/api/public/locations` (borough → neighborhoods map; fall back to repo file if empty)

Also read the repo’s canonical enums (do not guess):
- Categories, districts, ages, day/time tags, badges: `src/lib/curator/constants.ts`
- Neighborhoods per district: `src/data/locations.ts` (must match ingest)
- Discover activity filter chips: `src/data/catalogFilters.ts` (`ACTIVITY_TYPES`)
- Provider Zod shape: `src/lib/curator/providerSchema.ts`
- **Locale ingest rules (required):** `src/lib/curator/localeIngestRules.ts`
- App locales: `en`, `es`, `it`, `hu`, `he`, `ar` (`src/i18n/config.ts`)
- Example payloads: `scripts/ingest-payloads/*.json` (see `budapest-a38.json` for `locales.hu`)

Use the live public data to estimate which parts of the catalog are most underrepresented.

## Scarcity dimensions
Measure coverage across these dimensions using the live catalog:

### Provider categories
- Events
- Parties
- Restaurants
- Cafés

### Meetup group types (culture circles only)
- Art & Gallery
- Live Culture
- Food & Wine Circle
- Nightlife Crew
- Local Creators

When comparing scarcity, treat **providers** and **meetup groups** as separate catalogs unless you are deliberately filling a meetup gap.

### Districts (`borough` field — Budapest districts, not NYC boroughs)
- Belváros
- Terézváros
- Erzsébetváros
- Ferencváros
- Buda
- Óbuda
- Újbuda

### Neighborhoods
Use the app’s canonical neighborhood list from `GET /api/public/locations` or `src/data/locations.ts`. **Only** use neighborhoods that exist for that district in that data. Do not invent neighborhood names.

### Age / crowd tags (`ageRanges` on providers; `ageRange` on meetups)
- All ages
- Family
- 18+
- 21+
- Late night

### Day/time tags (`dayTimeTags`)
- Weekday
- Weekend
- Morning
- Afternoon
- Evening
- Late night

### Activity types (providers only)
Prefer tags from `src/data/catalogFilters.ts` when they fit the source:
- Live Music, DJ Sets, Rooftop, Ruin Bar, Fine Dining, Street Food, Wine Bar, Craft Beer, Coffee & Brunch, Gallery, Theatre, Festival, Boat Party, Thermal Bath, Jazz, Electronic, Cocktails, Late Kitchen

You may add another short label only if the official source clearly supports it and it helps discovery; do not spam tags.

## Scarcity-driven selection rule
Choose the next listing from the least-populated area of the catalog, using this ranking process:

1. Decide provider vs meetup based on what the source actually is.
2. Rank **category** (providers) or **groupType** (meetups) from fewest rows to most.
3. Within the scarcest type, rank **districts** from fewest rows to most.
4. Within that type + district, rank **neighborhoods** from fewest rows to most.
5. Within that slice, rank **age / crowd** tags from fewest to most.
6. Within that slice, rank **day/time** tags from fewest to most.
7. For providers, rank **activity types** from fewest to most.
8. Find a real official listing that improves the scarcest combination you can reasonably satisfy.

Important:
- Do not get stuck trying to satisfy a perfectly empty combination if no credible official listing exists.
- Use pragmatic fallback: if the exact scarcest combination is not realistically sourceable, move to the next scarcest nearby combination.
- Prefer real catalog coverage improvement over theoretical perfection.
- Avoid always picking the same famous ruin bars if a smaller valid official venue better fills a scarcer district / neighborhood / category slice.

## Candidate selection rules
1. Verify the source looks current and official.
2. Dedupe against the live public APIs by normalized **name**, proposed **`id`**, and **website** when present.
3. If the listing already exists, find a different one.
4. Prefer listings that improve coverage in the scarcest category/groupType + district + neighborhood + age + day-time + activity-type combination.
5. If two candidates are similarly scarce, prefer:
   - more official source
   - clearer district/neighborhood fit (canonical neighborhood only)
   - stronger age/crowd fit
   - better image availability for ImgBB upload
   - more specific activity typing from `ACTIVITY_TYPES`

## Data quality rules
1. Use only facts supported by the official source.
2. If something is unknown, leave it empty (`""`), `0`, or omit optional fields; list gaps under `missingOrUncertain`.
3. **`pricePerClass`**: typical per-person spend in **EUR** (cover, tasting menu, average main, ticket, or spa entry) — cite from the source only. If only a range exists, use the lower bound and note uncertainty in `missingOrUncertain` and `longDescription`.
4. **`rating` / `reviewCount`**: use `0` unless the source states explicit ratings or review counts; never fabricate TripAdvisor/Google numbers.
5. Do not guess neighborhood names; map to the closest **canonical** neighborhood only when the address or source supports that district.
6. **`id`**: providers must match `^prov-[a-z0-9-]+$` (e.g. `prov-mazel-tov-erzsebetvaros`). Meetups should use `grp-` prefix and a unique slug (e.g. `grp-danube-jazz-circle-belvaros`).
7. **`longDescription`**: must end with a line: `Sources: <url1> <url2> ...` (audit URLs; may duplicate `sourceUrls`).

## Multilingual copy (blocking for providers)
The public site serves **six locales**: `en`, `es`, `it`, `hu`, `he`, `ar`. Every **provider** upsert must ship translated copy for all non-English locales.

1. **Root `document` fields** (`name`, `shortDescription`, `longDescription`, `address`, optional announcements) are **English** — default fallback.
2. Add **`locales`** with keys **`hu`**, **`es`**, **`it`**, **`he`**, **`ar`** (do not omit any). Do **not** duplicate English under `locales.en`.
3. Each locale object **must** include:
   - `name` — venue name in that language (keep well-known brands if appropriate, e.g. “A38”)
   - `shortDescription` — 1–2 sentences
   - `longDescription` — full paragraph(s); must end with `Sources: <https://...>` (same audit URLs as English)
   - `slug` — lowercase ASCII hyphenated URL segment for `/hu/venue/{slug}`, `/es/venue/{slug}`, etc. (unique per provider; no accents in slug)
4. Optional per locale: `address` (localized line), `announcementTitle`, `announcementDescription`, `announcementBadge`, `image` (ImgBB only; usually omit).
5. **Keep `activityTypes` only on the root** in English (from `ACTIVITY_TYPES`). The app translates tags via message files — do **not** put `activityTypes` inside `locales.*`.
6. **Structural fields stay English on the root only:** `id`, `category`, `borough`, `neighborhood`, `ageRanges`, `dayTimeTags`, `badges`, `pricePerClass`, `rating`, `reviewCount`, `website`, `phone`, `email`.
7. **Hebrew / Arabic:** use natural RTL copy in text fields; `slug` stays Latin ASCII.
8. `npm run ingest:listing` **rejects** provider payloads missing any required locale or with invalid slugs.

## Image requirement (blocking)
1. Source an **official** image from the venue/organizer site or organization-controlled asset URL.
2. Download that image locally (e.g. under `scripts/imgbb-asset-sources/providers/<slug>.jpg`).
3. Upload through a supported repo flow (pick one):
   - `POST https://budapest-night.vercel.app/api/ingest/upload` with multipart field `file` and header `Authorization: Bearer <INGEST_API_KEY>` (or `X-Ingest-Key`)
   - `POST /api/admin/upload` if an admin session is available
   - Local: `npm run imgbb:upload-assets` only for bundled marketing assets; for new listings prefer `/api/ingest/upload` or direct ImgBB API with `IMGBB_API_KEY` from `.env.local`
4. Put the returned ImgBB `url` in `image` (provider) or `coverImageUrl` (meetup).
5. Record the original official image URL in `sourceUrls` or `notes`.
6. If upload fails, retry or choose another official image.
7. If you cannot produce a valid ImgBB URL for that listing, **do not ingest it**; choose another listing.

## Output shape
If it is a venue / program, produce JSON for `npm run ingest:listing`:

```json
{
  "sourceUrls": ["https://..."],
  "notes": "one-line why this listing fits scarcity goal",
  "missingOrUncertain": [],
  "operations": [
    {
      "resource": "provider",
      "action": "upsert",
      "document": { }
    }
  ]
}
```

The `document` must satisfy the provider schema and enums exactly, including:
- `category`, `borough`, `neighborhood` (canonical), `ageRanges[]`, `dayTimeTags[]`, `badges[]`, `activityTypes[]` (English canonical tags only)
- `announcementTitle`, `announcementDescription`, `announcementBadge` when useful
- non-empty https ImgBB `image`; `galleryImages` only if every entry is ImgBB
- **`locales`** with `hu`, `es`, `it`, `he`, `ar` each containing `name`, `shortDescription`, `longDescription`, `slug`

Minimal `locales` shape (fill all five keys in real payloads):

```json
"locales": {
  "hu": {
    "name": "…",
    "shortDescription": "…",
    "longDescription": "…\n\nSources: https://…",
    "slug": "venue-slug-hu"
  },
  "es": { "name": "…", "shortDescription": "…", "longDescription": "…\n\nSources: https://…", "slug": "venue-slug-es" },
  "it": { "name": "…", "shortDescription": "…", "longDescription": "…\n\nSources: https://…", "slug": "venue-slug-it" },
  "he": { "name": "…", "shortDescription": "…", "longDescription": "…\n\nSources: https://…", "slug": "venue-slug-he" },
  "ar": { "name": "…", "shortDescription": "…", "longDescription": "…\n\nSources: https://…", "slug": "venue-slug-ar" }
}
```

If it is a culture circle / community group:

```json
{
  "resource": "meetupGroup",
  "action": "upsert",
  "document": {
    "id": "grp-...",
    "name": "...",
    "borough": "...",
    "neighborhood": "...",
    "groupType": "Art & Gallery | Live Culture | Food & Wine Circle | Nightlife Crew | Local Creators",
    "ageRange": "...",
    "cadence": "Weekly | Monthly | Weekend | Pop-up",
    "instagram": "",
    "website": "https://...",
    "description": "...",
    "initials": "XX",
    "icon": "community | skyline | heart | coffee | stroller | playground",
    "palette": "teal | orange | beige | charcoal",
    "coverImageUrl": "https://i.ibb.co/..."
  }
}
```

## Write and ingest
From the repo root (`budapest-night`):

1. Save the JSON under:
   `scripts/ingest-payloads/cursor-curated-<short-slug>.json`
2. Ensure `INGEST_API_KEY` is set in `.env` or `.env.local` (see `.env.example`).
3. Dry-run:
   `npm run ingest:listing -- --dry-run scripts/ingest-payloads/cursor-curated-<short-slug>.json`
4. Ingest:
   `npm run ingest:listing -- scripts/ingest-payloads/cursor-curated-<short-slug>.json`
5. If this is a refresh of an existing id and dedupe blocks the run, re-run with:
   `npm run ingest:listing -- --force scripts/ingest-payloads/cursor-curated-<short-slug>.json`
6. Optional override host: `INGEST_BASE_URL=http://localhost:3000` for local API testing.
7. If ingest fails, fix the payload and retry until `HTTP 200` and `ok: true`.

## Final reply
After successful ingest, reply with:
- **What was added or updated**: name, id, category or groupType, district, neighborhood
- **Locales written**: confirm `hu`, `es`, `it`, `he`, `ar` slugs (and that `GET /api/public/providers?locale=hu` returns localized name for the new id)
- **Why chosen**: scarcest coverage opportunity you targeted
- **Scarcity summary**:
  - provider category counts (and meetup groupType counts if relevant)
  - chosen district count within that type
  - chosen neighborhood count within that type + district
  - chosen age/day-time/activity-type rationale
- **Source URLs** used
- **Original official image** source URL
- **Final ImgBB** URL stored in the payload
- **`missingOrUncertain`** items
- **Confirmation**: re-fetch the relevant public API row and confirm the image URL is present

Optional focus for this run (delete or edit one line):
- Category: Events / Parties / Restaurants / Cafés / Meetup culture circle / no preference
- District: Belváros / Terézváros / Erzsébetváros / Ferencváros / Buda / Óbuda / Újbuda / no preference
