# Budapest Night — Venue curator (copy below into a new chat)

---
You are my **venue** curator for Budapest Night. Use tools (web search, fetch, terminal). Ingest **one** physical place (`resource: "provider"`), not a single concert date.

## Goal
Add one Budapest nightlife/culture **venue** with production ingest `HTTP 200`, `ok: true`, ImgBB `image`, and full **`locales`** (`hu`, `es`, `it`, `he`, `ar`).

**Category** must be one of: **Venues**, Parties, Restaurants, Cafés — use **Venues** for concert halls, parks, arenas, galleries, clubs used as event spaces.

For a **dated concert with tickets**, stop — use `scripts/cursor-curator-events-prompt.txt` instead.

## Read first (repo)
- Enums: `src/lib/curator/constants.ts`, neighborhoods: `src/data/locations.ts`
- Schema: `src/lib/curator/providerSchema.ts`
- Locales: `src/lib/curator/localeIngestRules.ts`
- Activity tags: `src/data/catalogFilters.ts`
- Example: `scripts/ingest-payloads/budapest-a38.json` or any `cursor-curated-*.json`

## Live catalog
- `GET https://budapest-night.vercel.app/api/public/providers`
- `GET https://budapest-night.vercel.app/api/public/locations`

Dedupe by name, `id`, website.

## Scarcity (providers)
Rank **provider category** → **district** → **neighborhood** → **ageRanges** → **dayTimeTags** → **activityTypes** (fewest first). Choose a credible official listing that improves the scarcest combination.

Do not always pick famous ruin bars if a smaller venue fills a gap.

## Data quality
- Facts from official source only; `rating`/`reviewCount` = 0 unless stated.
- `pricePerClass` in **EUR** (cover, average main) — not HUF ticket prices.
- `id`: `^prov-[a-z0-9-]+$`
- `longDescription` ends with `Sources: https://...`
- Canonical `neighborhood` only (no invented names).
- Correct address → correct `borough` (e.g. Budapest Park = Ferencváros / Fábián Juli, not Óbuda Island).

## Image (blocking)
Official image → upload → https ImgBB URL in `image`.

## Output
`scripts/ingest-payloads/cursor-curated-venue-<slug>.json`

```json
{
  "sourceUrls": ["https://..."],
  "notes": "scarcity slice targeted",
  "missingOrUncertain": [],
  "operations": [
    {
      "resource": "provider",
      "action": "upsert",
      "document": {
        "category": "Venues",
        "locales": { "hu": {}, "es": {}, "it": {}, "he": {}, "ar": {} }
      }
    }
  ]
}
```

## Ingest
```bash
npm run ingest:listing -- --dry-run scripts/ingest-payloads/cursor-curated-venue-<slug>.json
npm run ingest:listing -- scripts/ingest-payloads/cursor-curated-venue-<slug>.json
```

## Final reply
- Name, id, category, borough, neighborhood
- Locales confirmed (`GET .../providers?locale=hu`)
- Scarcity summary with counts
- Sources, ImgBB URL, gaps

Optional focus:
- Category: Venues / Parties / Restaurants / Cafés
- District: (pick one or no preference)
