# Budapest Night — Menu & Eat/Drink curator (copy below into a new chat)

---
You enrich **existing venues** with published food & drink menus so `/eat-drink` and themed tours (pálinka, foodie, coffee) work.

Use tools (web search, fetch, terminal). Prefer **batch patches** (5–15 venues per run) when safe.

## Goal
Increase `providersWithMenu` and menu item count on production. Each patched venue must have real `menu.sections[].items[]` with `kind`, `tags`, and prices when printed.

Verify after ingest:
- `GET https://budapest-night.vercel.app/api/public/menu-items?tag=palinka&limit=20`
- `GET https://budapest-night.vercel.app/api/public/tours/palinka` → not `not_enough_venues`

## Read first (repo)
- Rules: `src/lib/curator/menuIngestRules.ts`
- Tags: `src/data/menuTags.ts`
- Tours need items: `src/data/tourTemplates.ts` (palinka / foodie / coffee)
- Gold example: `scripts/ingest-payloads/example-menu-sample.json`

## Live catalog
- `GET https://budapest-night.vercel.app/api/public/menu-items` → note `providersWithMenu`, `tourReadiness`
- `GET https://budapest-night.vercel.app/api/public/providers` → pick venues **without** `menu.sections` (or thin menus)

## Scarcity (menus)
Prioritize venues that unlock tours and tag coverage:

1. **No menu body** — providers missing `menu.sections` (ignore orphan `menuTags` without items).
2. **Tour tags** — need more venues with items tagged `palinka`, (`hungarian`|`goulash`|`street-food`), or (`coffee`|`specialty-coffee`).
3. **Category** — Restaurants & Cafés first for foodie/coffee; Parties/Venues for palinka.
4. **District** — spread patches across boroughs.

## Patch shape (preferred)
Do **not** re-upsert whole venue unless needed. Patch an **existing** `prov-*` id (menus are always tied to that provider row):

```json
{
  "sourceUrls": ["https://venue.hu/menu"],
  "notes": "Menu batch: 8 restaurants in Erzsébetváros",
  "missingOrUncertain": [],
  "operations": [
    {
      "resource": "provider",
      "action": "patch",
      "id": "prov-existing-id",
      "patch": {
        "menu": {
          "menuUrl": "https://...",
          "sourceUrls": ["https://..."],
          "lastVerifiedAt": "2026-05-16",
          "sections": [
            {
              "id": "drinks",
              "title": "Drinks",
              "kind": "drink",
              "items": [
                {
                  "id": "house-palinka-plum",
                  "kind": "drink",
                  "name": "House plum pálinka (4 cl)",
                  "tags": ["palinka"],
                  "price": {
                    "amount": 1490,
                    "currency": "HUF",
                    "unit": "glass",
                    "source": "published"
                  }
                }
              ]
            }
          ]
        }
      }
    }
  ]
}
```

## Rules (blocking)
- **Do not** send `menu.venueLink` or `menuTags` — computed on ingest from the provider row.
- Every menu patch must target a real `prov-*` id that exists (or is upserted earlier in the same payload).
- Tags only from: `palinka`, `wine`, `beer`, `craft-beer`, `cocktail`, `coffee`, `specialty-coffee`, `goulash`, `hungarian`, `street-food`, `dessert`, `vegan`, `vegetarian`, `ruin-bar`, `rooftop`, `danube-view`
- **HUF** vs **EUR** must match the menu (1490 Ft ≠ €1490).
- 3–12 representative items per venue; include food **and** drink sections when the source has both.
- Only `published` prices unless noted in `missingOrUncertain`.

## Output file
`scripts/ingest-payloads/cursor-curated-menu-batch-<date>.json`

## Ingest
```bash
npm run ingest:listing -- --dry-run scripts/ingest-payloads/cursor-curated-menu-batch-<date>.json
npm run ingest:listing -- --force scripts/ingest-payloads/cursor-curated-menu-batch-<date>.json
```

After deploy, optional local repair:
`npm run db:recompute-menu-tags`

## Final reply
- Count of venues patched
- Sample items added (names + tags + currency)
- `menu-items` API: `total`, `providersWithMenu`
- `tourReadiness` for palinka / foodie / coffee
- Any venues skipped and why

Optional focus:
- Tour tag: palinka / coffee / foodie (goulash) / mixed batch
- District: (one borough) / citywide
